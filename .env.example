# Copy to .env and fill in keys. The proxy reads this file itself (no dotenv package).

# Live Amass search + citation graph (required for live data / /api/amass proxy)
AMASS_API_KEY=

# Optional — Claude teaching + understanding check (extractive fallback otherwise)
ANTHROPIC_API_KEY=

# Optional — defaults to 3000
# PORT=3000
